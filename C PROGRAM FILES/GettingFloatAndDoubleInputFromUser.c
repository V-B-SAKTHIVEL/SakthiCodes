#include <stdio.h>
#include <conio.h>
void main()
{
	float floatnum;
	double doublenum;
	printf("ENTER A FLOAT VALUE AND A DOUBLE VALUE: ");
	scanf("%f %lf", &floatnum, &doublenum);
	printf("THE FLOAT VALUE IS: %f AND THE DOUBLE VALUE IS: %lf", floatnum, doublenum);
}
